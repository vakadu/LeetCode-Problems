// https://leetcode.com/problems/find-longest-special-substring-that-occurs-thrice-ii/description/

var maximumLength = function(s) {

};

function sortedSquarredArray(array){
    //write code here.make sure to return desired array
    const arr = array.map((val) => Math.pow(val, 2))
    console.log(arr);
}

sortedSquarredArray([1,3,5])

maximumLength("abcccccdddd");
